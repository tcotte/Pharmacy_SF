{% extends "default/index.html.twig" %}
{% block content %}
    Vous etes sur le formulaire
    {{ category.name }}

    <div class="col-md-5">
        {{form_start(form)}}
        <div class="form-row">
            <div class="form-group col-md-6">
                {{form_row(form.products)}}
            </div>
            <div class="form-group col-md-3">
                {{form_row(form.quantity)}}
            </div>
        </div>
        {{form_end(form)}}
    </div>
{% endblock %}
